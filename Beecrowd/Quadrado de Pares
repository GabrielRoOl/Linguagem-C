#include <stdio.h>
#include <math.h>

int main()
{
    int x, i, potencia=1;
    scanf("%d", &x);
    for(i = 1; i <= x; i++)
    {
        if(i % 2 == 0)
        {
            potencia = pow(i,2);
            printf("%d^2 = %d\n", i, potencia);
        }
    }
    
        return 0;
}
